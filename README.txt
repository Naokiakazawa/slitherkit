# Slither Docker Kit

## Docker commands
`docker run -it --rm -v ${pwd}/contracts/:/ slither`
`docker run -it --rm -v ${pwd}/contracts/:/ slither slither . --print contract-summary`

[Options](https://github.com/crytic/slither/wiki/Usage#options)

## slither version
v0.8.3